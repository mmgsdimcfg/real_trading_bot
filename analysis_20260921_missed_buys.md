# 20260921 매수 2건 원인 분석 (로그 기반)

작성 기준: `data_simulation/logs/20260921/r003_trade_live_execute_*.log` 3개 런(07:59, 08:08~12:41, 14:04~15:04),
10초 데이터 `data_simulation/data/<date>/<code>_*_10s.txt`. 분석 스크립트는 저장소에 넣지 않았음(임시 작업 폴더).

## 0. 결론
- 매수 2건(태광 08:08, 한화시스템 08:29)은 모두 **NXT 프리마켓(활성 14종목, 종목당 평가주기 ≈ 1.6초)** 에서 나왔고 **정규장(09:00~)은 0건**.
- 정규장에서 "모든 게이트 + 점수(≥10) 통과"(`[BUY HOLD]`, 1차 확인) 신호는 **215건**이었지만 **212건(98.6%)이 `_020` 연속 확인에서 폐기**됨.
  `_020`은 직전 통과와의 간격이 `BUY_CONFIRM_MAX_GAP_SECONDS`(=`POLL_INTERVAL_SECONDS*2` = 20초)를 넘으면 확인 횟수를 1로 되돌리는데,
  정규장 활성 50종목의 종목당 평가주기는 중앙 22.4초(97.2%가 20초 초과)라 두 번째 확인이 구조적으로 성립하지 않는다.
- 직접 측정: `_020` 재도달(연속 BUY HOLD) 간격 5일 425건 중 20초 이하는 **2건**, 20~30초 308건, 30~45초 95건, 45~60초 20건.
  같은 3분봉 안 94%, 두 통과 사이 가격 이탈 중앙 0.09%(p90 0.32%).
- 같은 패턴이 0914/0916/0917/0918에도 있음: 정규장 BUY HOLD 718건(독립 에피소드 293개) 중 ~95%가 다음 평가까지 20초 초과, 정규장 매수 0건.
- 정규장에서 `_021` 이후(매수 가능 수량/stale/호가) REJECT는 한 번도 나오지 않음(비-GATES REJECT는 SESSION_OPEN_WARMUP 412건뿐) - 확인 단계에서 전부 걸러졌기 때문.

## 1. 방법과 한계
- 각 `[REJECT]`(GATES+STEPS 포함 51,740건)의 시점 가격 기준, 이후 60분 내 +2.0% 먼저(TP) / -1.5% 먼저(SL) / 둘 다 아님(NEI)로 결과 라벨. edge = TP%×2.0 - SL%×1.5.
  같은 (종목, 3분봉)은 1건으로 dedupe. 다일자 풀링은 0914/0916/0917/0918/0921 정규장.
- 1분 트리거 완화는 10초→1분봉 재구성 후 `check_buy_condition_1min_hybrid_trigger`를 상수만 바꿔 리플레이(기준선 일치율 81%).
- **한계(Codex 사전 검토 지적 포함)**: (1) 결과 라벨은 사후 값이라 임계값 선택에 쓰면 look-ahead, 실제 청산(TP1 3%/트레일/하드스탑 1.7%)·체결·수수료와 다름
  (2) 10초 데이터로는 같은 10초 안의 TP/SL 선후 판정 불가 (3) 5일·소표본, 여러 값을 훑은 뒤 고른 행은 다중비교 편향
  (4) 0914~0918 로그는 코드 버전이 섞임 (5) NXT 프리마켓은 10초 데이터가 없어 라벨 불가 (6) "단독 차단" 표본은 조건부 표본이라 게이트의 인과 효과가 아님.

## 2. 정규장 게이트별 차단 (20260921, 정규장 33,829 스냅샷)
| 게이트 | 독립 실패율 | 단독 차단(다른 게이트 모두 통과) |
|---|---|---|
| stochastic_buy_signal | 89.3% | 643 |
| 1min_trigger | 69.8% | 289 |
| di_spread_min | 75.9% | 210 |
| min_entry_atr_volatility | 59.5% | 141 |
| bb_mid_downtrend_block | 34.5% | 0 |
| min_liquidity_safety | 37.6% | 22 |
| bb_upper_gap_min | 11.2% | 38 |
| bb_slope_rising | 5.5% | 23 |
- 트리거 통과 10,216건(30.2%) 중 7게이트+점수 통과 = BUY HOLD 215건(0.64%). 첫 실패 사유 1위는 `1MIN_NO_BB_MID_GOLDEN_CROSS` (전체 REJECT의 65%).

## 3. 게이트 유효성 (5일 풀링, 3분봉 dedupe 29,334건, base TP14.0/SL18.9/edge -0.4)
- `min_entry_atr_volatility`: 차단군 TP7.1/SL11.3 vs 통과군 TP27.6/SL34.1, 일자별 5/5에서 나쁜 종목을 걸러냄 -> 유지.
- `1min_trigger`: 차단군 edge +0.2 vs 통과군 -4.2 (일자별 5/5에서 차단군 >= 통과군) -> 트리거 통과 자체는 우위 없음.
- `stochastic / di_spread / liquidity / downtrend / upper_gap`: 일자별 방향 혼재 -> 유효성 불확실. `bb_slope_rising`은 단독 차단 6건뿐이라 영향 미미.

## 4. 완화 검토안 (적용하지 않음 - 별도 검증 후 결정)
| 조건 | 현재 | 검토안 | 근거(5일 풀링) | 판정 |
|---|---|---|---|---|
| `_020` 확인창 | 20초 | 45초 + 같은 3분봉 + 가격 이탈 ≤0.3% | 재도달 간격 중앙 24초, 20초 이하 2/425. 제안 규칙 통과 342/425(80%) | 구조 결함 - **설계 결정 필요**(전략 변경). 대안: 활성 종목 수↓/병렬 조회로 평가주기 ≤10초 |
| STOCH_D_BUY_MIN | 50 | 40 | K>D & 40≤D<50 근접실패 n=82, TP36.6/SL32.9 | 탐색적 - 표본외 검증 후 |
| HYBRID_1MIN_TRIGGER_LOOKBACK_BARS | 8 | 12 | 새로 통과 n=59, TP39.0/SL28.8 (16·24봉은 효과 감소) | 탐색적 - 표본외 검증 후 |
| K 상한 90 / 1분 추격 BB갭 / ATR 0.6% / 점수 10 | - | 유지 | K 90~96 n=87 TP23/SL49, 추격갭 단독차단 SL52%, ATR 차단군 TP11/SL22, 점수 6~9 근접 n=23 edge -28 | 완화 금지 |
| 캔들 -0.6 / BB갭 0.7 / DI 8 / 상단여유 0.10 | - | 제외 | n=23~33 소표본, 다중비교 위험 | 근거 부족 |
- 진행 순서 제안: (1) `_020` 재설계만 먼저 적용해 하루~이틀 관찰 - 이것만으로 하루 약 40~60개 독립 진입 에피소드가 열려 매수 빈도가 크게 늘어남(수익성 근거는 아님: 확인창 통과 진입 TP33.5/SL40.2 vs 1회 통과 즉시 TP34.6/SL37.7)
  (2) 그다음 P1/P2를 하나씩 g003/섀도로 검증 후 적용. 한 번에 여러 개를 풀면 원인 추적이 불가능하다.

## 5. 게이트 외 운영 요인
- 12:41:38~14:04:13(82.8분) 실행기 중단(keyboard_interrupt), 08:50~09:00 10분 공백(장 전환 대기).
- 감시 50종목 + 백업 50종목 회전(09:00~11:00은 활성 50종목만, 11:00:28 TIME_LIMIT_HARD로 50종목 일괄 교체).
- NXT 프리마켓은 NXT 거래 가능 14종목만 감시(평가주기 ≈ 2초라 확인창 성립 -> 매수 2건).
- SESSION_OPEN_WARMUP 차단 1,070건, INSUFFICIENT_BARS 342건(미미).

## 6. 기존 동작 그대로 둔 알려진 이슈 (리팩터링에서 고치지 않음)
1. `_020` 확인창(20초) < 정규장 평가주기(≈22초) - 위 0번.
2. 확인 상태가 `_021`(수량 0)/`_022`(stale)/`_023`(호가 얇음) 거절과 주문 실패 때 지워지지 않아 다음 틱에 이전 횟수가 이어질 수 있음(창을 늘리면 영향 커짐).
3. `hard_stop_daily_count`는 "연속"이 아니라 하루 누적 횟수(주석은 '연속 HARD_STOP').
4. 확인 상태에 `bar_time`을 저장하지만 비교하지 않음 -> 창을 늘리면 서로 다른 3분봉의 통과가 합산될 수 있음.
